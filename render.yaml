services:
  - type: web
    name: murasaki-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python Murasaki_bot.py
    autoDeploy: true
    disk:
      name: murasaki-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: BOT_TOKEN
        sync: false
